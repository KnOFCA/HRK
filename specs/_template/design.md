# <功能名称> —— 功能设计

> | 项目 | 值 |
> |---|---|
> | 对应规格 | [`spec.md`](spec.md) v0.1.0（`Draft`） |
> | 对应系统设计 | [`../../../DESIGN.md`](../../DESIGN.md) |
> | 决策记录 | [`docs/adr/`](../../docs/adr/README.md) |
> | 最近更新 | YYYY-MM-DD |
>
> **本设计不定义需求。** 字段、格式、错误码、判定条件与 workflow gate 的唯一事实源是 `spec.md`。
> 引用约定：`This design implements <REQ-ID>`。设计与规格冲突时，以规格为准（`agent.md` §2）。
> ADR 只在 `docs/adr/` 中定义，本文件只放索引。

---

## 1. 设计目标

TBD —— 本设计实现哪些 `<DOMAIN-FEATURE>-REQ-*`，遵循哪些既有架构约定（模块边界、命名规范、接口模式、目录结构）。

## 2. 模块划分

| 模块 | 职责 | 明确不做的事 | 新增 / 复用 | 代码位置 |
|---|---|---|---|---|
| TBD | | | | `src/...` |

## 3. 接口设计

与 `spec.md` §2.3 的接口契约一一对应；本表只给出**实现位置**，契约内容以 `spec.md` 为准。

| 规格条款 | 接口 | 实现位置 |
|---|---|---|
| `<DOMAIN-FEATURE>-REQ-001` | | |

## 4. 处理流程

```text
（待补充流程图 / 时序图）
```

| Workflow Step | 处理逻辑 | 涉及模块 | 对应规格条款 |
|---|---|---|---|
| S1 | | | `<DOMAIN-FEATURE>-REQ-001` |

## 5. 数据结构

> 若数据结构的字段定义属于需求（对外契约），其唯一事实源是 `spec.md`；本表只描述物化方式与持久化边界。

| 结构 | 字段 | 类型 | 约束 | 持久化 |
|---|---|---|---|---|
| TBD | | | | |

## 6. 失败路径处理

对应 `spec.md` §4 的每个异常场景，给出具体处理方式；触发条件以 `spec.md` 为准。

| 场景编号 | 场景 | 检测方式 | 处理方式 | 错误码 / 异常 | 恢复行为 |
|---|---|---|---|---|---|
| EX-1 | 非法输入 | | | | |
| EX-2 | 空输入 | | | | |
| EX-3 | 边界值 | | | | |
| EX-4 | 超时 | | | | |
| EX-5 | 依赖不可用 | | | | |
| EX-6 | 网络异常 | | | | |
| EX-7 | 外部服务失败 | | | | |
| EX-8 | 资源不足 | | | | |
| EX-9 | 恢复行为 | | | | |

## 7. 依赖

### 7.1 复用既有能力

| 能力 | 来源模块 | 用法 |
|---|---|---|
| TBD | | |

### 7.2 新增依赖评估

新增第三方依赖前必须确认：是否已有现成能力、是否确实必要、是否兼容现有环境、是否会增加构建成本、是否会带来许可证风险、是否会影响部署。

| 依赖 | 版本 | 用途 | 是否必要 | 兼容性 | 许可证 | 批准人 |
|---|---|---|---|---|---|---|
| （无） | | | | | | |

## 8. 兼容性与回归影响

| 影响面 | 是否变化 | 说明 | 回归测试 |
|---|---|---|---|
| 已有 API 行为 | 否 | | |
| 返回格式 | 否 | | |
| 默认值 | 否 | | |
| 错误码 | 否 | | |
| 文件格式 | 否 | | |
| 外部可见行为 | 否 | | |

## 9. 安全与性能设计

| 规格条款 | 要求（canonical） | 设计措施 | 验证方式 |
|---|---|---|---|
| TBD | | | |

## 10. 设计决策记录

本功能相关 ADR 唯一存放在 [`docs/adr/`](../../docs/adr/README.md)，本文件不重复定义：

| ADR | 决策摘要 | 关联规格条款 |
|---|---|---|
| [ADR-XXXX](../../docs/adr/README.md) | | |

## 11. 未采纳方案

| 方案 | 未采纳原因 |
|---|---|
| TBD | |

## 12. 剩余问题

| 编号 | 问题 | 影响 | 处理计划 |
|---|---|---|---|
| TBD | | | |
