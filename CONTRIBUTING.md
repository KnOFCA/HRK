# 贡献指南

本项目的贡献流程由 [`agent.md`](agent.md) 定义，本文件仅为可执行清单。
两者冲突时，以 `agent.md` 为准。

---

## 1. 强制工作流

所有非简单修改都必须遵循以下顺序，除有明确且合理的原因外不得跳过阶段：

```text
需求 → 分析 → 规格化 → 规格审查 → 任务规划 → 实现 → 验证 → 归档
```

**禁止**：需求发生变化后直接修改代码。
正确顺序是：需求变更 → 修改规格 → 审查规格 → 修改任务计划 → 修改代码 → 修改测试 → 验证 → 归档。

---

## 2. 修改前（阶段 A/B/C/D）

- [ ] 已阅读与本次改动相关的 `SPEC.md`、`specs/<domain>/<feature>/`、`DESIGN.md` 与现有测试。
- [ ] 已确认受影响的模块、接口与依赖关系。
- [ ] 已识别兼容性与回归风险。
- [ ] 已识别并显式提出模糊或缺失的需求；存在影响实现正确性的关键歧义时，**停止实现并先澄清规格**。
- [ ] 规格已满足质量门禁：清晰（唯一合理解释）、完整（正常 / 异常 / 边界 / 失败）、可验证。
- [ ] 每个重要需求都具备可客观验证的验收标准（禁止"性能好""正常工作""足够快"这类描述）。
- [ ] 需求已按 P0 / P1 / P2 分级，且不存在被忽略的 P0 要求。
- [ ] 任务已登记到 `specs/<domain>/<feature>/tasks.md` 与 `TASKS.md`，并标明：对应规格章节、受影响文件、实现内容、依赖关系、对应测试、完成条件。
- [ ] 任务已按依赖顺序排列（数据模型 → 核心逻辑 → 接口 → UI / 上层调用 → 集成测试）。

---

## 3. 实现时（阶段 E）

- [ ] 只实现已批准规格中的内容，不自行扩展需求。
- [ ] 除规格明确要求外，保持现有行为不变：不改 API 行为、返回格式、默认值、错误码、文件格式与任何外部可见行为。
- [ ] 遵循现有模块边界、命名规范、接口模式、设计模式和目录结构。
- [ ] 采用最小必要修改，不进行与任务无关的大规模重构。
- [ ] 发现与本任务无关的问题时，仅记录（见"待办与已知问题"），不在同一任务中顺手修复。
- [ ] 未在未确认的情况下新增第三方依赖；新增前已评估：是否已有现成能力、是否必要、环境兼容性、构建成本、许可证风险、部署影响。
- [ ] 已完整处理失败路径：输入错误、数据缺失、网络异常、超时、权限不足、资源失败、外部接口错误。
- [ ] 规格中定义的输入清洗、权限控制、频率限制、加密、内存 / 时间限制、并发要求均已实现，未被视为可选优化。

---

## 4. 提交前（阶段 F）

验证必须由规格驱动。对每一条验收标准：找到规格条款 → 找到实现 → 找到测试 → 执行 → 记录 PASS / FAIL。

推荐验证顺序：

1. 构建
2. 静态检查（lint / static analysis）
3. 单元测试
4. 模块测试
5. 集成测试
6. 回归测试
7. 验收标准检查
8. 无法自动化时进行人工验证

- [ ] 构建通过，且无 warning（如规格有此项要求）。
- [ ] 所有必须测试通过；`VALIDATION.md` 已记录实际执行的命令与结果。
- [ ] 每个验收标准的 PASS / FAIL 已逐条记录，未以"代码看起来没问题"或"部分测试通过"代替结论。
- [ ] 环境 / 工具导致的失败已明确标记 `BLOCKED`，未伪报为 PASS。
- [ ] `SPEC.md`、功能规格、设计与实现保持一致；未引入未记录的新行为。

---

## 5. 完成门禁

一个任务只有同时满足以下全部条件，才能标记为 `DONE`：

- 规格已更新
- 规格已经通过审查
- 实现与规格一致
- 所需测试已经存在
- 所有必须测试通过
- 所有验收标准通过
- 不存在已知 P0 问题
- 回归测试通过
- 文档保持一致
- 没有引入未记录的新行为

以下状态**不能**视为完成：代码已生成、代码已提交、能够编译、Demo 能运行、部分测试通过。

---

## 6. 结果状态

汇报与记录统一使用：

| 状态 | 含义 |
|---|---|
| `PASS` | 已验证通过 |
| `FAIL` | 已验证不通过 |
| `BLOCKED` | 因环境 / 工具 / 权限等外部原因无法验证 |

禁止使用"基本完成""应该没问题""大体可用"等模糊描述。

---

## 7. 验证失败处理

先判定失败类型，再按其流程处理：

| 类型 | 判定 | 处理方式 |
|---|---|---|
| A. 实现缺陷 | 规格正确，实现不符合规格 | 修改代码 → 重新验证 |
| B. 规格不完整 | 规格未定义某种场景 | 补充规格 → 重新审查 → 修改实现 → 修改测试 → 重新验证 |
| C. 规格错误 | 规格要求本身不可行、矛盾或错误 | 修改规格 → 重新审查 → 修改实现 → 重新验证 |
| D. 环境 / 工具问题 | 编译器缺失、SDK 版本错误、网络不可用、设备缺失、权限不足 | 标记 `BLOCKED`，不得报告为 PASS |

类型 C 下，**不得**通过修改代码去偷偷绕过错误规格。

---

## 8. 需求变更

新增或修改需求，一律遵循：

```text
需求变更 → 修改规格 → 审查规格 → 修改任务计划 → 修改代码 → 修改测试 → 验证 → 归档
```

以下变化都属于需求变更，必须先反映到规格中：新增功能、修改默认行为、修改接口、修改参数、修改错误处理、修改性能目标、修改兼容性、修改安全策略。

---

## 9. 命名与格式约定

- 功能目录：`specs/<domain>/<kebab-case-name>/`，详见 [`specs/README.md`](specs/README.md)。
- 规格条款编号：`<DOMAIN-FEATURE>-REQ/AC/TASK/TEST-<三位序号>`，在该功能内唯一且稳定，删除条款后不得复用编号。
- 每个功能目录必须同时包含五件套：`spec.md`、`design.md`、`tasks.md`、`test-plan.md`、`validation.md`。
- 每个功能规格必须声明**四维状态**（`spec_status` / `approval` / `implementation_status` / `validation_status` / `release_status`），
  取值与含义见 [`specs/README.md`](specs/README.md) §4。
- `Reviewed` 表示规格本身站得住，可由执行 Agent 填写；`Approved` 必须代表**真实授权**并留下
  `approver` / `approval_date` / `approval_evidence`。**禁止**执行者代签，无证据时只能写 `Approval: Pending`。
- 所有非简单修改在完成前必须运行 `tools/sdd/check.sh`，并修复或按 §7 标记 `BLOCKED`。
- 文档与源码使用 UTF-8 编码。

---

## 10. 可追溯性要求

重要需求必须能够回答：

- **为什么存在？** 对应哪个业务需求或规格条款。
- **在哪里实现？** 对应哪些源文件或模块。
- **如何验证？** 对应哪些测试。
- **哪个版本引入？** 对应哪个 Commit / Tag / Release。

推荐维护如下映射：

```text
SPEC-2.1
    ↓
src/...
    ↓
tests/...
    ↓
commit abc123
```

---

## 11. 待办与已知问题

与当前任务无关但已发现的问题记录在此（或对应功能规格的"剩余问题"章节），由后续任务按流程处理。

| 编号 | 问题描述 | 发现时间 | 影响 | 处理状态 |
|---|---|---|---|---|
| TBD | | | | |

---

## 12. 归档

功能通过验收后至少完成：

- [ ] 更新 `SPEC.md`
- [ ] 更新对应功能规格（`specs/<domain>/<feature>/`）
- [ ] 更新 `docs/changes/CHANGELOG.md`
- [ ] 记录重要设计决策（`DESIGN.md` 或功能 `design.md`）
- [ ] 将规格与对应代码版本建立关联
- [ ] 保存测试与验证结果（`VALIDATION.md`）

归档目标是：未来维护人员仅通过仓库资料即可回答"为什么有这个功能、它应该做什么、它是如何实现的、如何证明它是正确的、哪个版本实现了它、为什么后来发生了修改"。
