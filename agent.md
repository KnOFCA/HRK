# Agent 开发协议

本项目采用 **规格驱动开发（Specification-Driven Development, SDD）**。

规格文档是实现、测试、审查和维护过程中的唯一事实来源。

---

## 1. 核心原则

1. 先定义规格，再进行实现。
2. 不实现规格中未定义的行为或功能。
3. 每一个重要需求都必须具有可验证的验收标准。
4. 代码、测试和文档必须始终与规格保持一致。
5. 当需求发生变化时，必须先修改规格，再修改代码。
6. 优先采用满足规格要求的最小改动。
7. 不得静默修改架构、接口、依赖关系或既有行为。

优先级顺序：

规格正确性
> 功能正确性
> 回归安全性
> 可维护性
> 实现便利性

---

## 2. 唯一事实来源

当多个信息源之间存在冲突时，按照以下优先级判断：

1. **系统级规格** `SPEC.md`（cross-cutting constraint）
2. **功能级规格** `specs/<domain>/<feature>/spec.md`（本功能需求）
3. 功能级设计 `design.md` / 系统设计 `DESIGN.md`
4. 任务计划 / 测试计划
5. 项目工程规范与实现文档
6. 验证记录中的描述
7. `README.md`（只描述状态与导航）
8. Agent 自身推断

**系统级约束优先于功能级规格**：功能规格不得违反 `SPEC.md` 的 cross-cutting constraint。
完整的职责边界与优先级见 [`specs/README.md`](specs/README.md) §3。

如果现有实现与规格发生冲突，
默认以规格为准。

只有存在充分证据表明规格本身需要修改时，
才允许修改规格。

不得自行、静默地解决规格冲突。

存在无法确定的冲突时，应明确指出并停止相关实现。

---

## 3. 开发工作流

所有非简单修改均遵循以下流程：

需求
→ 分析
→ 规格化
→ 规格审查
→ 任务规划
→ 实现
→ 验证
→ 归档

除非有明确且合理的原因，否则不得跳过阶段。

---

## 4. 阶段 A —— 分析

在修改代码之前：

1. 理解用户的实际目标。
2. 阅读相关现有代码。
3. 阅读已有规格和测试。
4. 确定受影响的模块。
5. 确定相关接口和依赖关系。
6. 识别兼容性和回归风险。
7. 识别模糊或缺失的需求。

如果仍存在会影响实现正确性的关键歧义，
不得直接开始实现。

优先澄清或补充规格。

---

## 5. 阶段 B —— 规格化

创建或更新对应的功能规格。

每份规格至少需要定义以下内容。

### 功能需求

包括：

- 功能目的
- 用户场景
- 正常业务流程
- 预期行为

明确回答：

“系统需要做什么？”

### 输入 / 输出

包括：

- 输入内容
- 输出内容
- 数据类型
- 数据格式
- 值域范围
- 接口契约

明确回答：

“系统接收什么，并产生什么？”

### 约束条件

包括：

- 技术约束
- 兼容性要求
- 依赖约束
- 性能要求
- 安全要求

明确回答：

“实现必须遵守什么限制？”

### 边界与异常场景

至少考虑：

- 非法输入
- 空输入
- 边界值
- 超时
- 依赖不可用
- 网络异常
- 外部服务失败
- 资源不足
- 恢复行为

不得只描述理想路径。

### 验收标准

每一个重要需求都必须能够通过以下一种或多种方式验证：

- 自动化测试
- 手工测试
- 性能测量
- 静态检查
- 可重复的验证步骤

避免使用：

- “性能好”
- “用户体验友好”
- “足够快”
- “合理”
- “正常工作”
- “尽可能优化”

这类无法客观判断的描述。

应改写为：

- “95% 的请求响应时间小于 200 ms。”
- “输入为空时返回错误码 INVALID_ARGUMENT。”
- “连续失败 5 次后账号锁定 15 分钟。”
- “构建过程不得出现 warning。”

规格必须能够被验证。

---

## 6. 规格质量门禁

规格只有满足以下三个条件后，
才能进入实现阶段。

### 清晰

每条需求只有一种合理解释。

不得存在明显歧义。

### 完整

必须覆盖：

- 正常流程
- 异常流程
- 边界场景
- 失败行为

### 可验证

每条重要需求都必须能够转换为具体测试条件或验证步骤。

如果规格未通过质量门禁：

不得继续实现。

应首先完善规格。

---

## 7. 阶段 C —— 规格审查

在开始实现之前，对规格进行审查。

重点检查：

- 完整性
- 正确性
- 技术可行性
- 一致性
- 接口兼容性
- 边界场景
- 异常处理
- 安全性
- 性能
- 向后兼容性
- 可测试性

必要时对需求进行优先级分类。

推荐使用：

### P0

必须满足。

通常包括：

- 核心功能
- 数据正确性
- 安全要求
- 系统稳定性
- 不得违反的接口契约
- 关键兼容性要求

所有 P0 要求必须实现。

### P1

重要要求。

应当实现，但通常不会直接造成系统不可用。

例如：

- 性能优化
- 用户体验
- 可维护性增强
- 非关键兼容性

### P2

可选改进。

例如：

- 体验优化
- 非关键视觉调整
- 可选增强功能

不得因为时间或实现便利而忽略 P0 要求。

---

## 8. 阶段 D —— 任务规划

将已经批准的规格转换为具体实现任务。

每个任务至少需要标明：

- 对应规格章节
- 受影响文件
- 实现内容
- 依赖关系
- 对应测试
- 完成条件

例如：

| 任务 | 规格 | 文件 | 验证 |
|---|---|---|---|
| T1 | SPEC-2.1 | parser.cpp | parser_valid |
| T2 | SPEC-2.2 | parser.cpp | parser_invalid |
| T3 | SPEC-3.1 | limiter.cpp | rate_limit |

任务应按照依赖顺序执行。

例如：

数据模型
→ 核心逻辑
→ 接口
→ UI / 上层调用
→ 集成测试

不得在依赖尚未满足时盲目实现上层功能。

---

## 9. 阶段 E —— 实现

实现过程中必须遵循以下规则。

### 9.1 严格遵循规格

只实现已经批准的规格内容。

不得自行扩展需求。

不得因为：

“这样更完整”
“这个功能应该也需要”
“顺便优化一下”

而加入规格中不存在的行为。

---

### 9.2 保持现有行为

除非规格明确要求改变，
否则必须保持现有行为不变。

重点避免：

- 修改已有 API 行为
- 修改返回格式
- 修改默认值
- 修改错误码
- 修改文件格式
- 修改外部可见行为

---

### 9.3 遵循现有架构

优先遵循：

- 已有模块边界
- 已有命名规范
- 已有接口模式
- 已有设计模式
- 已有目录结构

不得为了局部方便破坏整体架构。

---

### 9.4 最小修改原则

优先：

最小必要修改

而不是：

大规模重构。

如果完成需求只需要修改 3 个文件，
不得无理由修改 20 个文件。

---

### 9.5 避免无关重构

本任务之外的问题：

可以记录。

但不得在同一任务中随意修复。

例如：

当前任务：

实现新的输入处理逻辑

发现：

旧模块命名不好

不应该顺便重命名整个模块。

除非该问题：

- 会阻止当前任务完成
- 会造成严重错误
- 规格明确要求修改

---

### 9.6 控制依赖

不得因为实现方便就增加新的第三方依赖。

新增依赖前需要确认：

- 是否已有现成能力
- 是否确实必要
- 是否兼容现有环境
- 是否会增加构建成本
- 是否会带来许可证风险
- 是否会影响部署

---

### 9.7 完整处理失败路径

不能只实现：

Happy Path。

还需要按照规格处理：

- 输入错误
- 数据缺失
- 网络异常
- 超时
- 权限不足
- 资源失败
- 外部接口错误

---

### 9.8 遵守安全与性能要求

如果规格中定义了：

- 输入清洗
- 权限控制
- 频率限制
- 加密
- 内存限制
- 时间限制
- 并发要求

则必须实现。

不得将其视为可选优化。

---

## 10. 阶段 F —— 验证

验证必须由规格驱动。

对于每一个验收标准：

1. 找到对应的规格条款。
2. 找到对应的实现。
3. 找到对应的测试。
4. 执行测试。
5. 记录 PASS / FAIL。

推荐验证顺序：

1. 构建
2. 静态检查
3. 单元测试
4. 模块测试
5. 集成测试
6. 回归测试
7. 验收标准检查
8. 无法自动化时进行人工验证

---

### 验证原则

不得因为：

“代码看起来没问题”

就认为任务完成。

不得因为：

“AI 已经生成代码”

就认为任务完成。

不得因为：

“某几个测试通过”

就认为所有规格均满足。

完成状态必须建立在验证证据之上。

---

## 11. 验证失败处理

当验证失败时，
首先判断失败属于哪一种类型。

### A. 实现缺陷

规格正确，
实现不符合规格。

处理方式：

修改代码
→ 重新验证

---

### B. 规格不完整

实现过程中发现规格没有定义某种场景。

处理方式：

补充规格
→ 重新审查
→ 修改实现
→ 修改测试
→ 重新验证

---

### C. 规格错误

规格中的要求本身不可行、矛盾或错误。

处理方式：

修改规格
→ 重新审查
→ 修改实现
→ 重新验证

不得通过修改代码去“偷偷绕过”错误规格。

---

### D. 环境 / 工具问题

例如：

- 编译器缺失
- SDK 版本错误
- 网络不可用
- 测试设备缺失
- 权限不足

应明确标记：

BLOCKED

而不是错误报告为：

PASS。

---

## 12. 需求变更流程

任何新增需求或需求修改均必须遵循：

需求变更
→ 修改规格
→ 审查规格
→ 修改任务计划
→ 修改代码
→ 修改测试
→ 验证
→ 归档

禁止：

需求发生变化
→ 直接修改代码

---

### 核心原则

先改规格，后改代码。

任何影响系统行为的修改，
都应首先反映到规格中。

例如：

- 新增功能
- 修改默认行为
- 修改接口
- 修改参数
- 修改错误处理
- 修改性能目标
- 修改兼容性
- 修改安全策略

---

## 13. 可追溯性

项目需要尽量保持以下关系：

需求
↕
规格
↕
设计
↕
实现
↕
测试
↕
提交 / 版本

重要需求应能够回答：

### 为什么存在？

对应哪个业务需求或规格。

### 在哪里实现？

对应哪些源文件或模块。

### 如何验证？

对应哪些测试。

### 哪个版本引入？

对应哪个 Commit / Tag / Release。

---

### 推荐建立映射关系

例如：

```text
SPEC-2.1
    ↓
src/parser.cpp
    ↓
tests/parser_valid.cpp
    ↓
commit abc123

或者：

SPEC-3.2 Rate Limit
    ↓
src/security/RateLimiter.cpp
    ↓
tests/security/test_rate_limit.cpp
```

## 14. 完成门禁

一个任务只有满足以下所有条件，
才能标记为 DONE。

- 规格已更新
- 规格已经通过审查
- 实现与规格一致
- 所需测试已经存在
- 所有必须测试通过
- 所有验收标准通过
- 不存在已知 P0 问题
- 回归测试通过
- 文档保持一致
- 没有引入未记录的新行为

以下状态不能视为完成：

- 代码已生成
- 代码已提交
- 能够编译
- Demo 能运行
- 部分测试通过

## 15. 归档

功能通过验收后，需要完成归档。

至少包括：

- 更新 SPEC.md
- 更新对应 Feature Spec
- 更新变更记录
- 记录重要设计决策
- 将规格与对应代码版本建立关联
- 保存测试和验证结果

### 归档目标

未来维护人员应该能够仅通过仓库资料回答：

为什么有这个功能？

它应该做什么？

它是如何实现的？

如何证明它是正确的？

哪个版本实现了它？

为什么后来发生了修改？

## 16. Agent 输出格式

对于较复杂的开发任务，
Agent 最终应按照以下结构汇报。

### 规格

说明本次实现对应哪些需求。

例如：

SPEC 2.1
SPEC 2.2
SPEC 3.1

### 计划

说明准备：

修改哪些模块
修改哪些文件
新增哪些测试

### 实现

说明实际完成的修改。

避免只写：

“已完成相关实现。”

应明确说明：

修改了什么
为什么修改
对应哪条规格

### 验证

列出实际执行的：

build
test
lint
static analysis
integration test
manual test

例如：

```text
Build: PASS

Unit Tests:
42 / 42 PASS

Integration Tests:
7 / 7 PASS

Acceptance:
SPEC-2.1 PASS
SPEC-2.2 PASS
SPEC-3.1 PASS
```

### 结果

统一使用：

PASS

FAIL

BLOCKED

不要使用模糊描述，例如：

“基本完成”
“应该没问题”
“大体可用”

### 剩余问题

记录：

已知限制
未解决的问题
无法验证的内容
后续工作
潜在风险

不得隐瞒失败或未验证内容。

## 17. 文档组织约定

功能按 `specs/<domain>/<feature>/` 组织，每个功能包含 spec、design、tasks、test-plan、validation 五件套。
系统导航见 [README](README.md)，详细规则见 [规格指南](specs/README.md)。

## 18. 推荐的完整 SDD 交付链

对于正式的软件版本迭代，
推荐使用：

```text
SPEC.md
    ↓
DESIGN.md
    ↓
TASKS.md
    ↓
Implementation
    ↓
TEST_PLAN.md
    ↓
VALIDATION.md
    ↓
CHANGELOG / Release
```

其中：

```text
SPEC.md
= What
定义“要实现什么”

DESIGN.md
= How
定义“准备如何实现”

TASKS.md
= Steps
定义“分哪些步骤实现”

TEST_PLAN.md
= How to prove
定义“如何证明正确”

VALIDATION.md
= Evidence
记录“实际验证结果”
```

## 19. Agent 执行状态机

Agent 应按照以下状态推进任务：

```text
用户需求
   ↓
ANALYZE
分析代码、规格、架构和风险
   ↓
SPECIFY
创建或修改规格
   ↓
REVIEW
检查清晰性、完整性、可验证性
   ↓
PLAN
生成实施任务和测试映射
   ↓
IMPLEMENT
最小化修改实现规格
   ↓
VERIFY
构建、测试并逐条验证规格
   ↓
   ┌──────── FAIL ────────┐
   │                      │
   ↓                      │
判断失败原因              │
   │                      │
Spec Gap?                 │
   │                      │
 YES                      │
   ↓                      │
修改规格                  │
   ↓                      │
重新 Review / Plan ───────┘

PASS
   ↓
ARCHIVE
归档规格、代码、测试和版本记录
```

## 20. 最终原则

本项目中的 Agent 必须始终遵循：

不要直接根据对话编写代码，而应根据规格编写代码。

不要根据直觉判断是否正确，而应根据验收标准判断。

需求发生变化时，先修改规格，再修改实现。

代码生成不是完成，验证通过才是完成。

规格、实现和测试必须始终保持一致。

---

## 21. SDD 治理（状态模型、职责边界与自动检查）

本节是 SDD 架构的**治理基线**；约定细节以 [`specs/README.md`](specs/README.md) 为准。

### 21.1 文档职责边界

`SPEC` 约束 `DESIGN`；`DESIGN` 不得定义 `SPEC`；`TASKS` 不得定义 `SPEC` 或 `DESIGN`；
`TEST_PLAN` 不得成为需求事实源；`VALIDATION` 只能记录证据；`README` 只能描述状态与导航。

同一事实只允许定义一次：字段清单、字段数量、输入 / 输出格式、错误码、状态定义、成功 / 失败判定、
workflow gate、验收标准。其他文档只能引用。

### 21.2 状态模型

功能状态是四个生命周期维度及独立审批字段，禁止用单个词（如 `Delivered` / "已交付"）覆盖多维事实：

- `spec_status`：`Draft` / `Ready-for-Review` / `Reviewed` / `Approved` / `Superseded`
- `approval`：`Pending` / `Approved`（`Approved` 必须有真实 `approver` / `approval_date` / `approval_evidence`）
- `implementation_status`：`NotStarted` / `InProgress` / `Complete`
- `validation_status`：`NotRun` / `PASS` / `FAIL` / `BLOCKED`
- `release_status`：`NotAccepted` / `Accepted` / `Released` / `Superseded`

**不可伪造审批**：`Reviewed` 表示"规格本身站得住"，可由执行 Agent 填写；
`Approved` 必须代表真实授权。禁止 Agent 既写规格、又把自己记录为 product owner approver。
没有真实批准证据时只能写 `Approval: Pending`。

### 21.3 工作流概念

区分 **Workflow Step**（`S1`…`Sn`）、**Validation Gate**（step 内的通过条件）、
**Action Step**（产生交付物）、**Recording Step**（只记录证据）。不得把三者混用。

### 21.4 证据与可追溯

- 任何正式 Validation 证据不得依赖可能被 `git gc` 清除的不可达 commit；
  必须建立稳定引用（`refs/tags/validation/<feature>/<date>`），或显式标注 `Historical evidence not reproducible`。
- 不得伪造 commit / timestamp / hash。
- 每次正式验证在 `evidence/<feature>/<run-id>/manifest.json` 记录规格 / 设计 / 实现 / 验证器版本与产物哈希。

### 21.5 自动检查

所有非简单修改在完成前必须运行：

```bash
tools/sdd/check.sh
```

退出码 `0` = 全部 PASS；`1` = 存在 FAIL；`2` = 存在 BLOCKED（无 FAIL）。
**缺失输入绝不等于 PASS**：验证器在输入缺失时必须给出 `FAIL` 或 `BLOCKED`。
检查未通过时，应修复问题，或按 §11 明确标记 `BLOCKED`，不得放宽检查使其变绿。
