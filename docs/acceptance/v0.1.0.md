# v0.1.0 迭代签收

- 签收状态：Accepted（保留已知问题）。
- 签收授权人：当前会话用户。
- 签收执行时间：2026-09-20T16:49:06.100784+08:00。
- 授权证据：用户明确指示“提交git并推送远程，然后签收本次迭代，把 LateInput 的原因、事件类型及判定影响列入后续优先整改项。”
- 远程仓库：git@github.com:KnOFCA/HRK.git。
- 已推送实现基线：`8e45be80d4acfcb6454859bd2e6c21717d4f14ec`，分支 `master`。
- 签收版本引用：`v0.1.0`（包含本记录的提交）。

## 签收范围

签收本轮规格范围内的实现、测试及证据交付。主机92项测试、CTest3组、原验收124 PASS及1个条件性不支持；真机自动补测、真人输入/听感/旋转和跨Backend Replay证据见 [验证记录](../../VALIDATION.md)。不代表应用商店发布，不作无缺陷或零输入丢弃承诺。

## 保留问题与后续优先整改

[任务索引](../../TASKS.md) 的 LI-001 至 LI-004 为 P1 下一迭代优先工作：定位 LateInput 原因、分类 DOWN/MOVE/UP/CANCEL、评估实际判定/分数及 pointer 生命周期影响，并实施经规格评审的修复与回归。如证实核心正确性缺陷升级 P0。

真人测试 LateInput17/3、overflow0 已披露；已接受 Replay 一致不证明被拒绝事件无影响。120Hz请求真机实际约90Hz，按条件用例不支持记录。Linux sanitizer CI 的远程执行状态另行核验，不能由本签收推定通过。

## 版本与证据

签收基线包含源码、规格、验证器、原始验收记录及哈希；签名秘密和构建产物不入 Git。历史 manifest 保留当时未提交状态，避免改写历史。旧 source-hashes 还记录当时的本地 Native `.cxx` 缓存，缓存已排除于本次提交；这些缓存项不视作可从源码仓库恢复的文件。Git 文本规范化可能改变工作树文本行尾，原始证据与历史快照按字节保留。可追溯版本以本记录的 Git 提交/标签和各次运行的原始内容哈希分别表达。
